# CLAUDE.md

## プロジェクト概要

Figmaデザインを元にしたWebサイト実装。

対象は以下を含む。

- LP（ランディングページ）
- コーポレートサイト
- 採用サイト
- サービスサイト

採用ポートフォリオとして公開することを前提とし、
コード品質・設計品質・保守性も評価対象とする。

フレームワークは使用せず、
Vanilla HTML / CSS / JavaScriptで実装する。

---

# Figma

Figmaファイル

（ここにURLまたはファイル名）

対象ページ

（対象ページ名）

Figma解析はFigma MCPを優先して使用する。

MCPで取得できない情報のみ画像・スクリーンショットを参考にする。

実装判断に迷った場合は、必ずFigmaデザインを優先する。

Figmaへのコメント追加・編集は行わない。

---

# 実装前に必ず確認すること

以下を確認してから実装を開始する。

- CLAUDE.md
- ディレクトリ構成
- Gitの状態
- 既存HTML
- 既存CSS
- 既存JavaScript
- 既存Component
- Figmaとの差分

既存プロジェクトを修正する場合は、新規実装より既存コードの流用を優先する。

---

# ページ構成

Figma解析後に整理する。

例

- TOP
- About
- Service
- Works
- Recruit
- News
- Blog
- Contact
- Thanks

必要に応じて追加・変更する。

---

# レスポンシブ

モバイルファースト

ブレークポイント

- 768px
- 1260px

必要に応じて追加する。

---

# CSS設計

軽量FLOCSS + BEM

## Foundation

プレフィックスなし

- reset
- variables

色・余白・フォントサイズはCSS変数で管理する。

直値は極力使用しない。

---

## Layout

プレフィックス

l-

レイアウトのみ担当する。

---

## Project

プレフィックス

p-

ページ固有のセクションで使用する。

他ページで再利用しないUIはComponentではなくProjectとして管理する。

---

## Component

プレフィックス

c-

再利用可能なUIのみ作成する。

新しいComponentを作る前に既存Componentで流用できないか確認する。

---

## Utility

プレフィックス

u-

1クラス1役割とする。

---

# BEM

Block

c-card

Element

c-card\_\_title

Modifier

c-card--large

ネストは1階層までとする。

---

# JavaScript

JavaScript操作用クラスは

js-

プレフィックスを使用する。

JavaScript用クラスにはCSSを適用しない。

既存JavaScript機能は依頼がない限り変更しない。

---

# ディレクトリ構成

```text
/
├── index.html
├── about/
├── service/
├── recruit/
├── contact/
├── thanks/
│
├── assets/
│   ├── img/
│   ├── svg/
│   ├── fonts/
│   └── favicon/
│
├── css/
│   ├── foundation/
│   ├── layout/
│   └── object/
│
├── js/
│
└── CLAUDE.md
```

必要に応じて変更する。

---

# 実装ルール

- CLAUDE.mdを遵守する
- Figmaデザインを忠実に再現する
- HTMLはセマンティックに記述する
- CSS変数を積極的に使用する
- remを基本単位とする
- モバイルファーストで実装する
- コードの重複を避ける
- Componentの共通化を優先する
- 保守性・再利用性・可読性を重視する
- 必要がない限りHTML構造を変更しない
- 必要がない限り既存クラス名を変更しない
- 新規Componentより既存Componentを優先して流用する
- 独自判断でデザインを変更しない
- 不明点は実装前に確認する

---

# 差分実装ルール

既存プロジェクトを修正する場合は以下を遵守する。

- 新規実装より既存コードを優先する
- HTML構造を極力維持する
- CSS設計を維持する
- JavaScript機能を壊さない
- レスポンシブ対応を維持する
- 不要なリファクタリングを行わない
- 必要最小限の変更でFigmaデザインを再現する

---

# Webサイト実装ルール

- 各ページの役割を明確にする
- 共通パーツを優先してComponent化する
- グローバルナビゲーションの一貫性を保つ
- 現在地表示（Active状態）を考慮する
- CTA導線を適切に配置する
- 情報設計を意識したレイアウトとする
- スクロールしやすい余白設計とする
- ホバー・スクロール演出は過度にならないよう配慮する
- フォームがある場合は入力しやすさを優先する

---

# 作業進行ルール

実装開始前に分析結果と実装方針を報告する。

重大な仕様確認が必要な場合を除き、
工程ごとに停止せず最後まで実装する。

実装完了後に変更内容をまとめて報告する。

---

# コードレビュー

実装完了後はセルフレビューを行う。

確認項目

- HTML
- CSS
- JavaScript
- 保守性
- Component設計
- アクセシビリティ
- SEO
- レスポンシブ
- パフォーマンス
- CTA導線
- UX
- Console Error
- 未使用CSS
- 未使用画像
- リンク切れ
- 画像容量

改善案があれば提示する。

---

# Gitルール

コミットは機能単位で行う。

コミットメッセージは日本語でよい。

コミット前に以下を確認する。

- git status
- 不要ファイル
- Console Error
- リンク切れ

例

- 基盤構築
- 共通パーツ実装
- TOPページ実装
- Aboutページ実装
- Serviceページ実装
- Recruitページ実装
- Contactページ実装
- フォーム実装
- アニメーション実装
- レスポンシブ対応
- コードレビュー対応
- CLAUDE.md更新

---

# ポートフォリオ掲載ルール

初回実装ではFigmaデザインを忠実に再現する。

実装完了後に必要であればブラッシュアップ案を提案する。

対象例

- 配色
- 写真素材
- コピー
- アイコン
- 装飾
- CTAデザイン
- UI表現
- アニメーション

初回実装ではコード品質・保守性・再現度を優先する。
